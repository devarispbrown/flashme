<h1>New Card</h1>

<%= form_for([@deck, @card]) do |form| %>
  <div>
    <%= form.label :front %>
    <%= form.text_field :front %>
    <%= form.label :back %>
    <%= form.text_field :back %>
  </div>
  <%= form.submit "New card" %>
<% end %>